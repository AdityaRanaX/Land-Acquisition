# ========================================================
# SIH26016 - NLAMS Environment Variables
# ========================================================

# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Database Connection
MONGODB_URI=mongodb://127.0.0.1:27017/nlams_db

# Security & JWT
JWT_SECRET=nlams_jwt_super_secret_key_sih26016_hackathon_2024
JWT_EXPIRES_IN=7d

# OTP & Email (Mock or SMTP)
EMAIL_SERVICE_MOCK=true
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
OTP_EXPIRY_MINUTES=10

# GIS & Map Services
VITE_MAP_TILE_PROVIDER=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
